<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow fixed-top">
            <div class="container-fluid">
                <h1 class="" href="#">Notes</h1>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav mx-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="/">Beranda</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/features">Fitur</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/contact">Kontak</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/about">Tentang</a>
                        </li>
                    </ul>
                    <div class="d-flex">
                        <% if (!user) { %>
                            <a class="btn btn-outline-light me-2 btn-hover" href="/login">Login</a>
                            <a class="btn btn-primary btn-hover" href="/signup">Sign Up</a>
                        <% } else { %>
                            <span class="text-light me-3">Halo, <strong class="text-gradient"> <%= user.fullName %> </strong> </span>
                            <form action="/logout" method="post">
                                <button type="submit" class="btn btn-hover">Logout</button>
                            </form>
                        <% } %>
                    </div>
                </div>
            </div>
        </nav>
        
    </header>

    <%- body %>

    <footer class="bg-dark text-white py-4 mt-5">
        <div class="container text-center">
            <p class="text-gradient">&copy; <%= new Date().getFullYear() %> Catatan. Semua hak dilindungi.</p>
            <ul class="list-inline">
                <li class="list-inline-item">
                    <a href="/contact" class="text-white text-decoration-none"><h1 style="font-size: medium;">Hubungi Kami</h1></a>
                </li>
            </ul>
            <div class="mt-3">
                <a href="#" class="text-white me-3">
                    <i class="bi bi-facebook"></i>
                </a>
                <a href="#" class="text-white me-3">
                    <i class="bi bi-twitter"></i>
                </a>
                <a href="#" class="text-white me-3">
                    <i class="bi bi-instagram"></i>
                </a>
            </div>
        </div>
    </footer>
    
    <script src="/js/bootstrap.bundle.min.js"></script>
    <script src="/js/scripts.js"></script>
</body>
</html>
